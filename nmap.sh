#!/bin/bash

ip=$(echo $1)
ports=$(echo $2)

nmap -T4 -A -p$ports $ip > Text/nmap

echo "---------------------------------------------------------------------------------------------------------------------------------"
